const c=window.KAYAN_CONFIG;const q=(s)=>document.querySelector(s);document.querySelectorAll('[data-email]').forEach(e=>{const value=c[e.dataset.email];e.textContent=value;e.href=`mailto:${value}`});document.querySelectorAll('[data-updated]').forEach(e=>e.textContent=c.updated);document.querySelectorAll('[data-publisher]').forEach(e=>e.textContent=c.publisher);const cards=q('#platform-cards');if(cards)cards.innerHTML=c.platforms.map(p=>`<article class="card"><h3>${p.name}</h3><p>Use Kayan TV on your device with sources you are authorised to access.</p><a class="status" href="${p.href}">${p.status} →</a></article>`).join('');const legal=q('[data-legal-notice]');if(legal)legal.textContent=c.legalNotice;
